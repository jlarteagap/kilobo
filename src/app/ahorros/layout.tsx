import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Metas de Ahorro | Kilo',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
