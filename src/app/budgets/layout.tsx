import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Presupuestos | Kilo",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
